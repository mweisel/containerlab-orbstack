---
- name: get and install packages
  ansible.builtin.apt:
    pkg:
      - bash-completion
      - curl
      - file
      - tree
      - htop
      - tmux
      - jq
    state: present
  become: true
